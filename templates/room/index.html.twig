{% extends 'base.html.twig' %}

{% block title %}Room index{% endblock %}

{% block body %}

<div class="header">
    <img src="{{ absolute_url(asset('images/room.jpg')) }}" class="responsive"  alt="321">
    <div class="container">
        <h1>Room List</h1>
        <a href="{{ path('room_new') }}">Create new</a>
        
    </div>
    
</div>

<div class="content container">

    
    
</b>
    <div class="rooms row">

        {% for room in rooms %}
        <div class="col-sm-6">
            <div class="room panel">
                <div class="image"><img src="{{ asset(room.Image) }}" alt=""></div>
                <h2>{{ room.Name }}</h2>
                <span class="measure"><div><b>Height:</b> {{ room.Height }}</div> </span>
                <span class="measure"><div><b>Depth:</b> {{ room.Depth }}</div> </span>
                <span class="measure"><div><b>Width:</b> {{ room.Width }}</div> </span>
                <div class="view">
                    <a href="{{ path('room_show', {'id': room.id}) }}">View</a>
                    <!-- <a href="{{ path('room_edit', {'id': room.id}) }}">View</a> -->
                </div>
            </div>
        </div>
        {% else %}
            <div>
                <span colspan="6">no records found</span>
            </div>
        {% endfor %}
    </div>
    

    

    

</div>
    
{% endblock %}
